# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Node
node_modules/

# 编辑器 / OS
.vscode/
.idea/
.DS_Store

# codeagent 运行期状态（不跟踪）—— 整目录屏蔽
# session.json / session.sql 现在写在 ~/.codeagent/,仓库内不再生成
.codeagent/


# eval run outputs (regenerated by `python3 eval/run_eval.py`)
eval/results/
